[package]
name = "desktop"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crisp8_core = { path = "../crisp8_core"}

[dependencies.sdl2]
version = "0.35.0"
features = ["bundled", "static-link"]